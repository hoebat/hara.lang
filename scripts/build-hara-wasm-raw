#!/usr/bin/env bash

set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

cargo build --manifest-path rust/raw/Cargo.toml --target wasm32-unknown-unknown --release
echo "Built $ROOT/rust/raw/target/wasm32-unknown-unknown/release/hara_wasm_raw.wasm"
