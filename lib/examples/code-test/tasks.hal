(ns example.code-test.tasks
  (:require [std.lib.task :as task]))

(deftask double-task
  {:template :default
   :doc "Doubles each input"
   :main {:fn (fn [value] (* 2 value))}})

(task/invoke double-task [1 2 3])
