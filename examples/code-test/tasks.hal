; std.task is Java-backed and remains callable from Hara.
(require 'std.task)

(deftask double-task
  {:template :default
   :doc "Doubles each input"
   :main {:fn (fn [value] (* 2 value))}})

(std.task/invoke double-task [1 2 3])
