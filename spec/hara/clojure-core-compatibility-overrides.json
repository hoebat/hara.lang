{
  "schema_version": 1,
  "renamed": [
    {"clojure": "contains?", "hara": "IFind/has?", "summary": "Protocol membership uses the Hara IFind/has? protocol method."}
  ],
  "changed": [
    {"clojure": "map", "hara": "map", "dimensions": ["arity", "evaluation", "return-type"], "summary": "Direct calls are lazy in both; Hara's one-argument transform is eager on ordinary collections and returns a vector, while preserving arrays and existing lazy sources."},
    {"clojure": "seq", "hara": "seq", "dimensions": ["accepted-inputs", "arity", "return-type"], "summary": "Hara exposes the iterator boundary explicitly and also supports applying a transform lazily as (seq transform source); it does not require Clojure ISeq."},
    {"clojure": "rest", "hara": "rest", "dimensions": ["empty-value", "return-type"], "summary": "Hara returns nil when no values remain; Clojure rest returns an empty sequence."},
    {"clojure": "partition", "hara": "partition", "dimensions": ["arity", "evaluation", "return-type"], "summary": "Hara direct calls are lazy, while its one-argument transform is eager and returns a vector (or array for array input); Clojure's one-argument form is a transducer."},
    {"clojure": "partition-all", "hara": "partition-all", "dimensions": ["arity", "evaluation", "return-type"], "summary": "Hara's one-argument form is an eager transform rather than a Clojure transducer."},
    {"clojure": "keys", "hara": "keys", "dimensions": ["return-type"], "summary": "Hara returns its runtime-neutral lazy Seq rather than Clojure's JVM sequence implementation."},
    {"clojure": "vals", "hara": "vals", "dimensions": ["return-type"], "summary": "Hara returns its runtime-neutral lazy Seq rather than Clojure's JVM sequence implementation."},
    {"clojure": "some", "hara": "any?", "dimensions": ["name", "return-value"], "summary": "Hara any? returns a boolean existential result; Clojure some returns the first truthy predicate result."},
    {"clojure": "empty?", "hara": "empty?", "dimensions": ["accepted-inputs", "evaluation"], "summary": "Hara checks through the iterator protocol and may inspect a one-shot iterator; Clojure uses seq semantics."},
    {"clojure": "let", "hara": "let", "dimensions": ["binding-evaluation"], "summary": "Hara evaluates initializers against the enclosing lexical environment in parallel; Clojure let bindings are sequential."},
    {"clojure": "defrecord", "hara": "defrecord", "dimensions": ["host-types", "interop", "implementation"], "summary": "Hara has no Clojure JVM record/class contract; portable immutable domain values use defstruct and protocols."},
    {"clojure": "eval", "hara": "eval", "dimensions": ["host", "capability", "environment"], "summary": "Hara evaluates through its context-local language runtime and does not expose Clojure compiler/JVM eval semantics."}
  ]
}
